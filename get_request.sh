#!/usr/bin/env bash

curl https:/stackoverflow.com > result.txt;
curl https:/stackoverflow.com > result.txt;
curl https:/stackoverflow.com > result.txt;
curl https:/stackoverflow.com > result.txt;
curl https:/stackoverflow.com > result.txt;

